Read the email addresses from /app/users.csv and identify every email address that appears more than once in the file. Write the result to /app/output.txt. The output file must contain each duplicated email address exactly once, with one email address per line, sorted in alphabetical order. If there are no duplicate email addresses, create /app/output.txt as an empty file. Generate the output directly from the current contents of /app/users.csv each time the task is executed so that any changes to the input file are reflected in the result. The output file must contain only the required email addresses with no additional text, blank lines, headings, comments, or other formatting.